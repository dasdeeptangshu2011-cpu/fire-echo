import { useEffect, useMemo, useRef, useState, type ReactNode } from 'react'
import { Activity, AlertTriangle, Battery, Bot, CheckCircle2, ChevronRight, CircleDot, Cpu, Download, Flame, Gauge, Grid3X3, History, Home as HomeIcon, Info, Layers3, Map as MapIcon, Menu, Pause, Play, Radio, RefreshCw, Route, Satellite, Search, Settings, ShieldCheck, Signal, SlidersHorizontal, Thermometer, Wifi, WifiOff, X, Zap } from 'lucide-react'
import './App.css'

type Source='REAL'|'CALCULATED'|'ESTIMATED'|'SIMULATION'|'OFFLINE/REPLAY';type Mode='LIVE HARDWARE'|'SIMULATION'|'REPLAY';type Conn='DISCONNECTED'|'SCANNING'|'CONNECTING'|'CONNECTED'|'DEGRADED'|'CONNECTION LOST';type Page='Home'|'Live Map'|'3D Digital Twin'|'Events'|'Incident Command'|'Robot Control'|'Sentinel Network'|'Hardware Center'|'Simulation Lab'|'Analytics'|'Research Mode'|'System Health'|'Calibration'|'Data History'|'Settings'|'About'
type Telemetry={type:'sentinel_telemetry';deviceId:string;timestamp:number;temperature:number;humidity:number;smoke:number;flame:boolean;battery:number;signal:number;quality?:number};type Sentinel={id:string;x:number;y:number;baseline:{temperature:number;humidity:number;smoke:number};telemetry?:Telemetry;health:'ONLINE'|'DEGRADED'|'OFFLINE';source:Source};type Robot={x:number;y:number;battery:number;mission:string;connected:boolean;motor:'READY'|'MOVING'|'STOPPED';obstacle:boolean;ack:string;source:Source};type Analysis={state:string;confidence:number;x:number;y:number;uncertainty:number;support:string[];evidence:{label:string;points:number;detail:string}[]}
type BleCharacteristic={startNotifications:()=>Promise<void>;addEventListener:(type:string,fn:(e:Event)=>void)=>void;writeValue:(v:ArrayBuffer)=>Promise<void>};type BluetoothDevice={name?:string;gatt?:{connect:()=>Promise<{getPrimaryService:(u:string)=>Promise<{getCharacteristic:(u:string)=>Promise<BleCharacteristic>}>}>};addEventListener?:(t:string,fn:()=>void)=>void}
const UUID={service:'7b6f0001-6d1e-4e5a-9a31-464952452d01',telemetry:'7b6f0002-6d1e-4e5a-9a31-464952452d01',command:'7b6f0003-6d1e-4e5a-9a31-464952452d01'}
const initial:Sentinel[]=[['S01',2,3,29,68,8],['S02',8,3,30,64,9],['S03',15,4,30,62,10],['S04',4,13,29,71,7],['S05',10,12,30,63,10],['S06',17,14,30,66,8]].map(([id,x,y,t,h,s])=>({id:String(id),x:Number(x),y:Number(y),baseline:{temperature:Number(t),humidity:Number(h),smoke:Number(s)},health:'OFFLINE',source:'REAL'}))
const pages:Page[]=['Home','Live Map','3D Digital Twin','Events','Incident Command','Robot Control','Sentinel Network','Hardware Center','Simulation Lab','Analytics','Research Mode','System Health','Calibration','Data History','Settings','About']
const groups:Record<Page,string>={Home:'COMMAND','Live Map':'COMMAND','3D Digital Twin':'COMMAND',Events:'COMMAND','Incident Command':'COMMAND','Robot Control:'FIELD'} as Record<Page,string>
Object.assign(groups,{ 'Robot Control':'FIELD','Sentinel Network':'FIELD','Hardware Center':'FIELD','Simulation Lab':'LAB',Analytics:'LAB','Research Mode':'LAB','System Health':'SYSTEM',Calibration:'SYSTEM','Data History':'SYSTEM',Settings:'SYSTEM',About:'SYSTEM'})
const icons:Record<Page,(p:{size?:number})=>JSX.Element>={Home:HomeIcon,'Live Map':MapIcon,'3D Digital Twin':Grid3X3,Events:AlertTriangle,'Incident Command':ShieldCheck,'Robot Control':Bot,'Sentinel Network':Satellite,'Hardware Center':Cpu,'Simulation Lab':Activity,Analytics:Layers3,'Research Mode':Search,'System Health':Radio,Calibration:SlidersHorizontal,'Data History':History,Settings:Settings,About:Info}
const clamp=(n:number,a:number,b:number)=>Math.max(a,Math.min(b,n));const risk=(s:Sentinel)=>{const t=s.telemetry;if(!t)return 0;return clamp((t.temperature-s.baseline.temperature)*9+Math.max(0,s.baseline.humidity-t.humidity)*.8+Math.max(0,t.smoke-s.baseline.smoke)*1.8+(t.flame?32:0),0,100)}
function analyse(ss:Sentinel[]):Analysis{const r=ss.map(risk),support=ss.filter(s=>risk(s)>=30).map(s=>s.id),m=Math.max(0,...r);let state='NORMAL';if(m>=15)state='ENVIRONMENTAL ANOMALY';if(m>=30&&support.length<2)state='SUSPICIOUS EVENT';if(m>=30&&support.length>=2)state='CORROBORATED EVENT';if(m>=55)state='PROBABLE FIRE EVENT';if(m>=72)state='LOCALIZED EVENT';const w=r.map(v=>Math.max(.01,v)),sum=w.reduce((a,b)=>a+b,0),x=ss.reduce((a,s,i)=>a+s.x*w[i],0)/sum,y=ss.reduce((a,s,i)=>a+s.y*w[i],0)/sum,ta=Math.max(0,...ss.map(s=>s.telemetry?s.telemetry.temperature-s.baseline.temperature:0)),ha=Math.max(0,...ss.map(s=>s.telemetry?s.baseline.humidity-s.telemetry.humidity:0)),sa=Math.max(0,...ss.map(s=>s.telemetry?s.telemetry.smoke-s.baseline.smoke:0));const evidence=[['Temperature anomaly',clamp(Math.round(ta*3),0,18),`Peak +${ta.toFixed(1)}°C`],['Humidity anomaly',clamp(Math.round(ha*.9),0,12),`Peak −${ha.toFixed(1)}% RH`],['Smoke anomaly',clamp(Math.round(sa*.65),0,20),`Peak +${sa.toFixed(0)} index`],['Neighbour confirmation',clamp(support.length*5,0,15),`${support.length} supporting node(s)`],['Persistence / consistency',support.length>=2?10:0,support.length>=2?'Multiple observations agree':'Not established']].map(e=>({label:e[0] as string,points:e[1] as number,detail:e[2] as string}));return{state,confidence:clamp(evidence.reduce((a,e)=>a+e.points,0),0,100),x,y,uncertainty:clamp(8.5-support.length*.75-m*.045,2.2,8.5),support,evidence}}
function Metric({label,value,source}:{label:string;value:string;source:Source}){return <div className="metric"><div><span>{label}</span><small>{source}</small></div><b>{value}</b></div>};function Panel({title,icon,children}:{title:string;icon?:ReactNode;children:ReactNode}){return <div className="panel"><div className="panelTitle"><h2>{icon}{title}</h2></div>{children}</div>};function Stat({icon,label,value,source}:{icon:ReactNode;label:string;value:string;source:Source}){return <div className="stat"><div className="statIcon">{icon}</div><div><small>{label}</small><strong>{value}</strong><em>{source}</em></div></div>};function Empty({text}:{text:string}){return <div className="empty"><WifiOff size={17}/>{text}</div>};function Slider({label,value,set}:{label:string;value:number;set:(n:number)=>void}){return <label className="slider"><span>{label}<b>{value}</b></span><input type="range" min="0" max="100" value={value} onChange={e=>set(Number(e.target.value))}/></label>}
function ForestMap({ss,robot,event,selected,onSelect}:{ss:Sentinel[];robot:Robot;event:Analysis;selected:string|null;onSelect:(id:string)=>void}){return <div className="forestMap"><div className="mapLegend"><span>● SENTINEL</span><span>◆ EVENT ESTIMATE</span><span>■ ROBOT</span></div>{Array.from({length:16},(_,i)=><div className="sector" key={i}>B{i+1}</div>)}{ss.map(s=><button className={`mapNode ${risk(s)>50?'risk':''} ${selected===s.id?'selected':''}`} key={s.id} style={{left:`${s.x/20*100}%`,top:`${s.y/20*100}%`}} onClick={()=>onSelect(s.id)}>{s.id.slice(1)}</button>)}<div className="uncertainty" style={{left:`${event.x/20*100}%`,top:`${event.y/20*100}%`,width:`${event.uncertainty/20*200}%`,height:`${event.uncertainty/20*200}%`}}/><div className="eventPin" style={{left:`${event.x/20*100}%`,top:`${event.y/20*100}%`}}><Flame size={15}/></div><div className="robotPin" style={{left:`${robot.x/20*100}%`,top:`${robot.y/20*100}%`}}><Bot size={15}/></div><div className="mapAxes"><span>0m</span><span>10m</span><span>20m</span></div></div>}
function Health({label,value,good}:{label:string;value:string;good:boolean}){return <div className="healthRow"><span>{label}</span><b className={good?'pass':'fail'}>{good?'✓':'—'} {value}</b></div>}

export default function App(){const [page,setPage]=useState<Page>('Home'),[mode,setMode]=useState<Mode>('SIMULATION'),[running,setRunning]=useState(true),[ss,setSs]=useState(initial),[robot,setRobot]=useState<Robot>({x:10,y:18,battery:100,mission:'STANDBY',connected:false,motor:'READY',obstacle:false,ack:'NO COMMAND SENT',source:'SIMULATION'}),[gateway,setGateway]=useState<Conn>('DISCONNECTED'),[toast,setToast]=useState(''),[selected,setSelected]=useState<string|null>(null),[intensity,setIntensity]=useState(0),[wind,setWind]=useState(28),[dryness,setDryness]=useState(38),[fail,setFail]=useState(''),[weak,setWeak]=useState(''),[records,setRecords]=useState<Record<string,unknown>[]>(()=>{try{return JSON.parse(localStorage.getItem('fire-echo-records')||'[]')}catch{return[]}}),[cal,setCal]=useState({temperature:0,humidity:0,smoke:0}),[demo,setDemo]=useState(0);const cmd=useRef<BleCharacteristic|null>(null),event=useMemo(()=>analyse(ss),[ss]);const notify=(m:string)=>{setToast(m);window.setTimeout(()=>setToast(''),2400)}
useEffect(()=>{try{localStorage.setItem('fire-echo-records',JSON.stringify(records.slice(-500)))}catch{}},[records])
useEffect(()=>{if(mode!=='SIMULATION'||!running)return;const timer=window.setInterval(()=>{setSs(prev=>prev.map((s,i)=>{if(fail===s.id)return{...s,health:'OFFLINE',source:'SIMULATION'};const hot=i===2||i===4,boost=hot?intensity*.09+demo*.8:0,t=clamp(s.baseline.temperature+.3+boost+Math.sin(Date.now()/1500+i)*.15,20,55),h=clamp(s.baseline.humidity-(hot?intensity*.42+demo*.7:0),20,90),sm=clamp(s.baseline.smoke+(hot?intensity*1.25+demo*2:0),0,500),tel:Telemetry={type:'sentinel_telemetry',deviceId:s.id,timestamp:Date.now(),temperature:t+cal.temperature,humidity:h+cal.humidity,smoke:sm+cal.smoke,flame:hot&&intensity>65&&demo>2,battery:Math.max(5,(s.telemetry?.battery??96)-.004),signal:weak===s.id?12:88,quality:weak===s.id?20:96};return{...s,telemetry:tel,health:weak===s.id?'DEGRADED':'ONLINE',source:'SIMULATION'}}));setRobot(r=>({...r,source:'SIMULATION',battery:Math.max(5,r.battery-.002)}))},900);return()=>clearInterval(timer)},[mode,running,intensity,cal,fail,weak,demo])
const onTelemetry=(e:Event)=>{const v=(e.target as {value?:DataView}).value;if(!v)return;try{const p=JSON.parse(new TextDecoder().decode(new Uint8Array(v.buffer,v.byteOffset,v.byteLength))) as Partial<Telemetry>&Partial<Robot>;if(p.type==='sentinel_telemetry'&&typeof p.deviceId==='string'&&[p.timestamp,p.temperature,p.humidity,p.smoke,p.battery,p.signal].every(x=>typeof x==='number'&&Number.isFinite(x))){setSs(prev=>prev.map(s=>s.id===p.deviceId?{...s,telemetry:p as Telemetry,health:'ONLINE',source:'REAL'}:s));setRecords(prev=>[...prev,{timestamp:p.timestamp,deviceId:p.deviceId,temperature:p.temperature,humidity:p.humidity,smoke:p.smoke,battery:p.battery,signal:p.signal,source:'REAL'}])}else if(p.type==='robot_telemetry'){setRobot(r=>({...r,...p,connected:true,source:'REAL'} as Robot))}}catch{notify('Malformed telemetry rejected.')}}
const connect=async()=>{const bt=(navigator as Navigator&{bluetooth?:{requestDevice:(o:unknown)=>Promise<BluetoothDevice>}}).bluetooth;if(!bt){notify('Hardware communication is unavailable in this browser.');return}try{setGateway('SCANNING');const d=await bt.requestDevice({filters:[{services:[UUID.service]}],optionalServices:[UUID.service]});setGateway('CONNECTING');const server=await d.gatt?.connect();if(!server)throw new Error('GATT unavailable');const service=await server.getPrimaryService(UUID.service),tele=await service.getCharacteristic(UUID.telemetry);cmd.current=await service.getCharacteristic(UUID.command);await tele.startNotifications();tele.addEventListener('characteristicvaluechanged',onTelemetry);d.addEventListener?.('gattserverdisconnected',()=>{setGateway('CONNECTION LOST');setSs(p=>p.map(s=>({...s,health:'OFFLINE',source:'REAL'})));notify('Gateway disconnected.')});setGateway('CONNECTED');setMode('LIVE HARDWARE');notify(`Connected: ${d.name||'FIRE-ECHO gateway'}`)}catch(e){setGateway('DISCONNECTED');notify(e instanceof Error?e.message:'BLE connection failed or cancelled.')}}
const send=async(command:string,target?:{x:number;y:number})=>{if(mode!=='LIVE HARDWARE'||gateway!=='CONNECTED'||!cmd.current){notify('Command not sent: physical gateway is not connected.');return}try{await cmd.current.writeValue(new TextEncoder().encode(JSON.stringify({type:'robot_command',command,...target?{target}:{}})).buffer);setRobot(r=>({...r,ack:'WAITING FOR REAL ACK'}));notify(`Command transmitted: ${command}`)}catch{notify('Command transmission failed.')}}
const dispatch=()=>{if(mode==='SIMULATION'){setRobot(r=>({...r,mission:`VERIFY ${event.x.toFixed(1)},${event.y.toFixed(1)}`,ack:'SIMULATION ACK'}));setDemo(Math.max(demo,6));notify('Simulation mission queued.')}else void send('START_MISSION',{x:event.x,y:event.y})};const reset=()=>{setSs(initial);setIntensity(0);setDemo(0);setRobot(r=>({...r,x:10,y:18,mission:'STANDBY',ack:'NO COMMAND SENT'}));notify('Simulation reset.')};const exportData=(kind:'csv'|'json')=>{const data=kind==='json'?JSON.stringify(records,null,2):['timestamp,deviceId,temperature,humidity,smoke,battery,signal,source',...records.map(r=>Object.values(r).join(','))].join('\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([data]));a.download=`fire-echo.${kind}`;a.click();notify(`Exported ${records.length} record(s).`)};const source:Source=mode==='LIVE HARDWARE'?'REAL':mode==='REPLAY'?'OFFLINE/REPLAY':'SIMULATION',online=ss.filter(s=>s.health==='ONLINE').length,map=<ForestMap ss={ss} robot={robot} event={event} selected={selected} onSelect={setSelected}/>;const I=icons[page]
return <div className="app"><header className="topbar"><button className="mobileMenu" onClick={()=>document.querySelector('.sidebar')?.classList.toggle('open')}><Menu/></button><div className="brand"><span className="brandMark"><Flame size={20}/></span>FIRE-ECHO <em>2.0</em></div><div className="topState"><i className={event.state==='NORMAL'?'safe':''}/>{event.state}<b>{event.confidence}%</b></div><div className="modeSwitch">{(['LIVE HARDWARE','SIMULATION','REPLAY'] as Mode[]).map(m=><button className={mode===m?'selected':''} key={m} onClick={()=>{if(m==='LIVE HARDWARE'&&gateway!=='CONNECTED'){notify('Connect the real ESP32 gateway first.');return}setMode(m);if(m==='SIMULATION'){setSs(initial);setRunning(true)}}}>{m}</button>)}</div></header><aside className="sidebar"><div className="sidebarHeader">COMMAND SYSTEM<button onClick={()=>document.querySelector('.sidebar')?.classList.remove('open')}><X/></button></div>{['COMMAND','FIELD','LAB','SYSTEM'].map(g=><div className="navGroup" key={g}><small>{g}</small>{pages.filter(p=>groups[p]===g).map(p=>{const Icon=icons[p];return <button className={page===p?'navItem active':'navItem'} key={p} onClick={()=>{setPage(p);document.querySelector('.sidebar')?.classList.remove('open')}}><Icon size={16}/>{p}<ChevronRight size={12}/></button>})}</div>)}<div className="sidebarFooter"><span>GATEWAY <b>{gateway}</b></span><span>DATA <b>{source}</b></span></div></aside><main className="content"><div className="pageHeader"><div><div className="eyebrow">FIRE-ECHO / {page.toUpperCase()}</div><h1>{page}</h1><p>{desc(page)}</p></div><span className="sourceBadge"><CircleDot size={12}/> {source}</span></div>
{page==='Home'&&<><section className="statGrid"><Stat icon={<Satellite/>} label="ACTIVE SENTINELS" value={`${online}/${ss.length}`} source={source}/><Stat icon={<ShieldCheck/>} label="EVENT CONFIDENCE" value={`${event.confidence}%`} source="CALCULATED"/><Stat icon={<MapIcon/>} label="LOCALIZATION" value={`±${event.uncertainty.toFixed(1)}m`} source="ESTIMATED"/><Stat icon={<Bot/>} label="ROBOT" value={robot.mission} source={robot.source}/></section><section className="heroGrid"><Panel title="FOREST SITUATIONAL AWARENESS" icon={<MapIcon size={16}/>}><>{map}</></Panel><Panel title="SYSTEM CONCLUSION" icon={<ShieldCheck size={16}/>}><h2>{event.state}</h2><div className="confidenceRing"><b>{event.confidence}</b><span>RULE-BASED<br/>SCORE</span></div><button className="primary wide" disabled={event.state==='NORMAL'} onClick={dispatch}><Bot/> SEND FOR VERIFICATION</button><p className="micro">The score is not a scientifically validated fire probability.</p></Panel></section><section className="threeGrid"><Panel title="WHY ENGINE" icon={<ShieldCheck/>}>{event.evidence.map(e=><div className="evidence" key={e.label}><span>{e.label}</span><b>+{e.points}</b></div>)}</Panel><Panel title="NETWORK" icon={<Radio/>}><Metric label="Online" value={`${online}/${ss.length}`} source="CALCULATED"/><Metric label="Gateway" value={gateway} source={gateway==='CONNECTED'?'REAL':'OFFLINE/REPLAY'}/></Panel><Panel title="ACTIONS" icon={<Zap/>}><button className="actionRow" onClick={()=>setPage('Live Map')}>Open map <ChevronRight/></button><button className="actionRow" onClick={()=>setPage('Hardware Center')}>Test hardware <ChevronRight/></button><button className="actionRow" onClick={()=>setPage('Simulation Lab')}>Run simulation <ChevronRight/></button></Panel></section></>}
{page==='Live Map'&&<section className="twoCol"><Panel title="INTERACTIVE FOREST MAP" icon={<MapIcon/>}>{map}</Panel><Panel title="LOCALIZATION"><Metric label="X" value={`${event.x.toFixed(2)}m`} source="ESTIMATED"/><Metric label="Y" value={`${event.y.toFixed(2)}m`} source="ESTIMATED"/><Metric label="Uncertainty" value={`±${event.uncertainty.toFixed(2)}m`} source="ESTIMATED"/><h4>SUPPORT</h4>{event.support.length?event.support.map(id=><button className="nodeRow" key={id} onClick={()=>setSelected(id)}><Satellite/>{id}<ChevronRight/></button>):<Empty text="No corroborating nodes."/>}</Panel></section>}
{page==='3D Digital Twin'&&<Panel title="3D FOREST DIGITAL TWIN" icon={<Grid3X3/>}><div className="twinScene">{Array.from({length:44},(_,i)=><span className="tree3d" key={i} style={{left:`${4+(i*37)%92}%`,top:`${15+(i*23)%70}%`,transform:`scale(${.55+(i%5)*.12})`}}>▲</span>)}{ss.map(s=><div className="tower3d" key={s.id} style={{left:`${s.x/20*100}%`,top:`${18+s.y/20*65}%`}}>{s.id}</div>)}<div className="event3d" style={{left:`${event.x/20*100}%`,top:`${18+event.y/20*65}%`}}><Flame/></div><div className="robot3d" style={{left:`${robot.x/20*100}%`,top:`${18+robot.y/20*65}%`}}><Bot/></div><div className="twinHud"><b>20 × 20m</b><span>SHARED COORDINATE SYSTEM</span><b>{mode}</b></div></div><p className="micro">Spatial objects use the same coordinates as the map, localization and robot.</p></Panel>}
{page==='Events'&&<section className="twoCol"><Panel title="EVENT STATE MACHINE" icon={<Route/>}>{['NORMAL','ENVIRONMENTAL ANOMALY','SUSPICIOUS EVENT','CORROBORATED EVENT','PROBABLE FIRE EVENT','LOCALIZED EVENT'].map(s=><div className={event.state===s?'stage current':'stage'} key={s}><CircleDot size={12}/>{s}</div>)}</Panel><Panel title="EVIDENCE" icon={<ShieldCheck/>}>{event.evidence.map(e=><div className="evidence" key={e.label}><div><b>{e.label}</b><span>{e.detail}</span></div><strong>+{e.points}</strong></div>)}</Panel></section>}
{page==='Incident Command'&&<Panel title="INCIDENT COMMAND" icon={<ShieldCheck/>}><div className="statGrid"><Stat icon={<MapIcon/>} label="LOCATION" value={`(${event.x.toFixed(1)},${event.y.toFixed(1)})`} source="ESTIMATED"/><Stat icon={<Gauge/>} label="UNCERTAINTY" value={`±${event.uncertainty.toFixed(1)}m`} source="ESTIMATED"/><Stat icon={<ShieldCheck/>} label="CONFIDENCE" value={`${event.confidence}%`} source="CALCULATED"/></div><div className="decisionGrid"><button className="primary" onClick={dispatch}><Bot/> SEND FOR VERIFICATION</button><button className="dangerBtn" onClick={()=>void send('STOP')}><Pause/> STOP ROBOT</button><button className="ghost" onClick={()=>void send('RETURN_HOME')}><Route/> RETURN HOME</button></div></Panel>}
{page==='Robot Control'&&<section className="twoCol"><Panel title="ROBOT CONTROL CENTER" icon={<Bot/>}><div className="robotVisual"><Bot size={55}/></div><div className="statGrid compact"><Stat icon={<Wifi/>} label="CONNECTION" value={mode==='SIMULATION'?'SIMULATION':robot.connected?'CONNECTED':'OFFLINE'} source={robot.source}/><Stat icon={<Battery/>} label="BATTERY" value={`${robot.battery.toFixed(0)}%`} source={robot.source}/><Stat icon={<MapIcon/>} label="POSITION" value={`${robot.x.toFixed(1)},${robot.y.toFixed(1)}`} source={robot.source}/><Stat icon={<Signal/>} label="OBSTACLE" value={robot.obstacle?'DETECTED':'CLEAR'} source={robot.source}/></div>{['START','PAUSE','RESUME','STOP','RETURN_HOME','VERIFY_TARGET'].map(c=><button className={c==='STOP'?'dangerBtn':'ghost'} key={c} onClick={()=>c==='VERIFY_TARGET'?dispatch():void send(c)}>{c}</button>)}<div className="ack">ACK: {robot.ack}</div></Panel><Panel title="MISSION"><Metric label="Target" value={`(${event.x.toFixed(1)},${event.y.toFixed(1)})m`} source="ESTIMATED"/><p className="micro">The phone sends missions; the ESP32 robot handles motors and obstacle sensing locally.</p></Panel></section>}
{page==='Sentinel Network'&&<Panel title="SENTINEL NETWORK" icon={<Satellite/>}><div className="sentinelTable"><div className="tableHead"><span>NODE</span><span>STATUS</span><span>TEMP</span><span>RH</span><span>SMOKE</span><span>RISK</span><span>BATTERY</span><span>SOURCE</span></div>{ss.map(s=><button className="tableRow" key={s.id} onClick={()=>setSelected(s.id)}><b>{s.id}</b><span>{s.health}</span><span>{s.telemetry?`${s.telemetry.temperature.toFixed(1)}°C`:'—'}</span><span>{s.telemetry?`${s.telemetry.humidity.toFixed(0)}%`:'—'}</span><span>{s.telemetry?s.telemetry.smoke.toFixed(0):'—'}</span><span>{risk(s).toFixed(0)}</span><span>{s.telemetry?`${s.telemetry.battery.toFixed(0)}%`:'—'}</span><span>{s.source}</span></button>)}</div></Panel>}
{page==='Hardware Center'&&<section className="twoCol"><Panel title="ESP32 GATEWAY" icon={<Cpu/>}><div className="connectionHero"><Cpu size={36}/><h2>{gateway}</h2><p>Android ↔ ESP32 Gateway via BLE</p></div><button className="primary wide" onClick={connect}>{gateway==='SCANNING'?'SCANNING…':'SCAN FOR FIRE-ECHO GATEWAY'}</button><p className="micro">No live data or connection is fabricated.</p></Panel><Panel title="HARDWARE TEST" icon={<CheckCircle2/>}>{['BLE connection','Gateway telemetry','Sentinel telemetry','Robot telemetry','Command channel'].map(x=><div className="testRow" key={x}><span>{x}</span><b className={gateway==='CONNECTED'?'pass':'fail'}>{gateway==='CONNECTED'?'✓ PASS':'— WAITING'}</b></div>)}</Panel></section>}
{page==='Simulation Lab'&&<section className="twoCol"><Panel title="ENVIRONMENT SIMULATION" icon={<SlidersHorizontal/>}><div className="simulationRibbon"><Zap/> SIMULATION MODE — NO HARDWARE DATA</div><Slider label="Event intensity" value={intensity} set={setIntensity}/><Slider label="Wind" value={wind} set={setWind}/><Slider label="Dryness" value={dryness} set={setDryness}/><div className="buttonLine"><button className="primary" onClick={()=>setRunning(!running)}>{running?<Pause/>:<Play/>}{running?'PAUSE':'RUN'}</button><button className="ghost" onClick={reset}><RefreshCw/>RESET</button></div><select value={fail} onChange={e=>setFail(e.target.value)}><option value="">No sensor failure</option>{ss.map(s=><option key={s.id} value={s.id}>{s.id} offline</option>)}</select><select value={weak} onChange={e=>setWeak(e.target.value)}><option value="">No weak link</option>{ss.map(s=><option key={s.id} value={s.id}>{s.id} weak signal</option>)}</select></Panel><Panel title="COMPETITION DEMO" icon={<Play/>}>{['NORMAL FOREST','TEMPERATURE RISE','HUMIDITY FALL','SMOKE ANOMALY','NEIGHBOUR CONFIRMATION','EVENT LOCALIZED','ROBOT DISPATCH','ROBOT VERIFICATION'].map((x,i)=><button className={i<=demo?'demoStep done':'demoStep'} key={x} onClick={()=>{setDemo(i);setIntensity(Math.min(100,20+i*12));setRunning(true)}}>{String(i+1).padStart(2,'0')} {x}</button>)}<p className="micro">Safe digital demonstration only.</p></Panel></section>}
{page==='Analytics'&&<section className="panel"><div className="statGrid"><Stat icon={<Thermometer/>} label="AVG TEMP" value={`${(ss.reduce((a,s)=>a+(s.telemetry?.temperature||0),0)/Math.max(1,online)).toFixed(1)}°C`} source="CALCULATED"/><Stat icon={<Gauge/>} label="AVG RH" value={`${(ss.reduce((a,s)=>a+(s.telemetry?.humidity||0),0)/Math.max(1,online)).toFixed(1)}%`} source="CALCULATED"/><Stat icon={<Activity/>} label="LOCAL RECORDS" value={`${records.length}`} source="OFFLINE/REPLAY"/><Stat icon={<ShieldCheck/>} label="SCORE" value={`${event.confidence}%`} source="CALCULATED"/></div><div className="bars">{ss.map(s=><div className="barCol" key={s.id}><b>{risk(s).toFixed(0)}</b><i style={{height:`${risk(s)}%`}}/><span>{s.id}</span></div>)}</div></section>}
{page==='Research Mode'&&<section className="twoCol"><Panel title="METHOD" icon={<Search/>}><h3>Weighted spatial estimation</h3><pre>{`wᵢ = anomaly × reliability\nx̂ = Σ(wᵢxᵢ) / Σwᵢ\nŷ = Σ(wᵢyᵢ) / Σwᵢ`}</pre><p>FIRE-ECHO uses local baselines and transparent rule-based evidence. Confidence is not a validated fire probability.</p></Panel><Panel title="CURRENT RESULT"><Metric label="State" value={event.state} source="CALCULATED"/><Metric label="Location" value={`(${event.x.toFixed(2)},${event.y.toFixed(2)})m`} source="ESTIMATED"/><Metric label="Uncertainty" value={`±${event.uncertainty.toFixed(2)}m`} source="ESTIMATED"/><Metric label="Support" value={event.support.join(', ')||'None'} source="CALCULATED"/></Panel></section>}
{page==='System Health'&&<section className="twoCol"><Panel title="SYSTEM HEALTH" icon={<Radio/>}><Health label="Gateway" value={gateway} good={gateway==='CONNECTED'}/><Health label="Sentinels" value={`${online}/${ss.length} online`} good={mode==='SIMULATION'||online>0}/><Health label="Robot" value={mode==='SIMULATION'?'SIMULATION':robot.connected?'CONNECTED':'OFFLINE'} good={mode==='SIMULATION'||robot.connected}/><Health label="Data source" value={source} good/></Panel><Panel title="DATA QUALITY"><Metric label="Offline nodes" value={`${ss.filter(s=>s.health!=='ONLINE').length}`} source="CALCULATED"/><p className="micro">Stale or disconnected hardware is never represented as live.</p></Panel></section>}
{page==='Calibration'&&<section className="twoCol"><Panel title="LOCAL SENSOR OFFSETS" icon={<SlidersHorizontal/>}><label>Temperature <input type="number" value={cal.temperature} onChange={e=>setCal({...cal,temperature:Number(e.target.value)})}/></label><label>Humidity <input type="number" value={cal.humidity} onChange={e=>setCal({...cal,humidity:Number(e.target.value)})}/></label><label>Smoke <input type="number" value={cal.smoke} onChange={e=>setCal({...cal,smoke:Number(e.target.value)})}/></label><p className="micro">Stored locally in this browser.</p></Panel><Panel title="CALIBRATION RULE">Raw values remain distinguishable; offsets are configuration, not fabricated hardware measurements.</Panel></section>}
{page==='Data History'&&<Panel title="LOCAL DATA HISTORY" icon={<History/>}><div className="buttonLine"><button className="ghost" onClick={()=>exportData('csv')}><Download/>CSV</button><button className="ghost" onClick={()=>exportData('json')}><Download/>JSON</button><button className="dangerBtn" onClick={()=>setRecords([])}>CLEAR</button></div>{records.length?<div className="historyTable">{records.slice(-100).reverse().map((r,i)=><div className="historyRow" key={i}>{Object.entries(r).map(([k,v])=><span key={k}>{k}: {String(v)}</span>)}</div>)}</div>:<Empty text="No stored telemetry records yet."/>}</Panel>}
{page==='Settings'&&<section className="twoCol"><Panel title="FOREST MODEL" icon={<Grid3X3/>}><Metric label="Width" value="20m" source="CALCULATED"/><Metric label="Height" value="20m" source="CALCULATED"/><Metric label="Origin" value="(0,0)" source="CALCULATED"/><Metric label="Grid" value="4 × 4 sectors" source="CALCULATED"/></Panel><Panel title="OPERATING MODE" icon={<Settings/>}>{(['LIVE HARDWARE','SIMULATION','REPLAY'] as Mode[]).map(m=><button className={mode===m?'modeCard active':'modeCard'} key={m} onClick={()=>m==='LIVE HARDWARE'?void connect():setMode(m)}><b>{m}</b><span>{m==='LIVE HARDWARE'?'Only real telemetry':m==='SIMULATION'?'Safe generated telemetry':'Recorded local data'}</span></button>)}</Panel></section>}
{page==='About'&&<section className="about"><div className="aboutHero"><div className="brandMark large"><Flame size={38}/></div><span className="eyebrow">ENVIRONMENTAL INTELLIGENCE PLATFORM</span><h2>FIRE-ECHO 2.0</h2><p>Distributed sensing • transparent fusion • spatial estimation • operator-led verification</p></div><div className="threeGrid"><Panel title="TRUTHFUL DATA" icon={<ShieldCheck/>}><p>Every reading is explicitly identified by provenance.</p></Panel><Panel title="HARDWARE PATH" icon={<Cpu/>}><p>ESP32 sentinels and robot use documented BLE and JSON telemetry.</p></Panel><Panel title="SAFE DEMO" icon={<Activity/>}><p>Competition demonstrations use digital environmental changes, never real fire.</p></Panel></div></section>}
</main>{selected&&<div className="selectionDrawer"><button onClick={()=>setSelected(null)}><X/></button><span className="eyebrow">SELECTED NODE</span><h3>{selected}</h3>{ss.find(s=>s.id===selected)?.telemetry?<>{(()=>{const s=ss.find(x=>x.id===selected)!;const t=s.telemetry!;return <><Metric label="Temperature" value={`${t.temperature.toFixed(1)}°C`} source={s.source}/><Metric label="Humidity" value={`${t.humidity.toFixed(1)}%`} source={s.source}/><Metric label="Smoke" value={`${t.smoke.toFixed(0)}`} source={s.source}/><Metric label="Battery" value={`${t.battery.toFixed(0)}%`} source={s.source}/></>})()}</>:<Empty text="NO LIVE HARDWARE DATA"/>}</div>}{toast&&<div className="toast"><CheckCircle2 size={16}/>{toast}</div>}</div>}
function desc(p:Page){const d:Record<Page,string>={Home:'Distributed environmental intelligence command center','Live Map':'Coordinate-based localization and information gaps','3D Digital Twin':'Data-driven spatial forest model',Events:'Transparent event state machine and evidence trail','Incident Command':'Operator-led incident verification','Robot Control':'Physical verification robot command interface','Sentinel Network':'Node telemetry, baselines and health','Hardware Center':'ESP32 gateway and BLE diagnostics','Simulation Lab':'Safe simulation and what-if testing',Analytics:'Environmental trends and network analytics','Research Mode':'Methods, assumptions and mathematical transparency','System Health':'Connection and data-quality monitoring',Calibration:'Local sensor calibration offsets','Data History':'Locally stored telemetry records',Settings:'Forest model and operating mode',About:'FIRE-ECHO architecture and project information'};return d[p]}
